#include <stdio.h>
int main() {
   int i, j, rows;
   printf("Enter the number of rows: ");
   scanf("%d", &rows);
for(i=1;i<=row;i++)
{
for(j=0;j<=i;j++)
printf("%d",i);
}
printf("\n");
}
return 0;
}

